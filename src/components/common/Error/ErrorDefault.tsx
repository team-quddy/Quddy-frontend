import styled from "styled-components";

const ErrorDefault = () => {
  return <ErrorComponent>페이지를 불러오는 것을 실패했습니다.</ErrorComponent>;
};

const ErrorComponent = styled.div``;

export default ErrorDefault;
